# Проект SkyPro_Bank

## Описание:

Проект SkyPro_Bank - это программа для анализа банковский действий, которая пишется учениками курса SkyPro

## Установка:

1. Клонируйте репозиторий:
```
git clone https://github.com/hatber91/SkyPro_Bank.git
```
2. Установите зависимости:
```
pip install -r requirements.txt
```
## Использование:

В проекте реализованы следующие возможности:
- Поиск транзакций по описанию с использованием регулярных выражений;
- Подсчёт количества операция по категориям с использованием Counter;
- Работа с пользовательским меню через функцию main;
- Обработка данных из JSON, CSV и XLSX файлов.

### Возможности:
- считывание финансовых операций из JSON-файла;
- считывание финансовых операций из CSV-файла;
- считывание финансовых операций из Excel-файла.

## Тестирование:

В проекте настроено тестирование с помощью `pytest`.

Для запуска тестов выполните команду:

```
pytest
```
Тестами покрыты основные функции проекта:

- маскирование номера банковской карты; 
- маскирование номера счёта; 
- обработка входной строки с картой или счётом;
- преобразование даты в формат ДД.ММ.ГГГГ;
- фильтрация операций по статусу;
- сортировка операций по дате;
- фильтрация транзакций по валюте;
- генератор описаний транзакций;
- генератор номеров банковских карт;
- декоратор логирования log;
- Тестами покрыты функции чтения транзакций из CSV- и Excel-файлов.
Для тестирования используется Mock и patch, чтобы исключить зависимость от реальных файлов.

В проекте протестированы:

- корректность работы функций;
- обработку пустых данных;
- корректная работа генераторов;
- обработка пустых списков;
- проверка крайних значений диапазонов;
- корректность форматирования данных;
- обработка случаев отсутствия совпадающих значений;
- вывод логов в консоль;
- запись логов в файл;
- корректную работу декораторов.

### Также в проекте используется:
- mypy - для проверки типизации;
- flake8 - для проверки качества и стиля кода.
- capsys - используется фикстура для тестирования вывода в консоль   

## Документация:

Для получения дополнительной информации обратитесь к [документации](docs/README.md).

## Лицензия:

Этот проект лицензирован по [лицензии MIT](LICENSE).